#include <bits/stdc++.h>
using namespace std;
#define ll long long int

void fun()
{
	ll n, answer=0;
	for(int i=0; i<4; i++)
	{
		cin >> n;
		if(n>=10)
			answer += 1;
	}
	cout<<answer;
}

int main()
{
	ll T=1;
	while (T--)
	{
		fun();
	}
}